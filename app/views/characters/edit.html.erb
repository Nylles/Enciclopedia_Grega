<%= form_for @character do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :photo_url %>
  <%= f.text_field :photo_url %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :story %>
  <%= f.text_area :story %>

  <%= f.submit %>
<% end %>
